export enum MessagePortType {
	Inport = 0,
	Outport = 1,
	Undefined = 2
}

export enum TransportState {
	STOPPED = 0,
	RUNNING = 1
}

// Deprecated aliases (minified names from original bundle)
export { MessagePortType as s, TransportState as S }
